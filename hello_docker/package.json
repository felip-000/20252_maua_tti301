{
  "name": "hello_docker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "When you're ready, start your application by running:\r `docker compose up --build`.",
  "dependencies": {
    "express": "^5.1.0"
  }
}
